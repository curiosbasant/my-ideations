import type { PropsWithChildren } from 'react'

export const metadata = {
  title: 'Dots and Boxes',
}

export default function DotsAndBoxesLayout(props: PropsWithChildren) {
  return props.children
}
